# 03 — Run the first observable Application Scenario

**What to build:** Let a developer launch one unpackaged Application Scenario that creates a fresh Scenario Sandbox, drives a visible application action through WebdriverIO, records a replayable Run Event Log, captures useful evidence, and leaves no product process behind.

**Blocked by:** 01 — Make the application automation-accessible

**Status:** ready-for-human

- [x] The scenario launches from the project runner on Linux; Windows runtime verification is deferred to the future Windows test pass.
- [x] It uses fresh isolated application state and never reads or changes a real profile.
- [x] A user-visible action is performed through accessible UI semantics.
- [x] Validated versioned JSONL events describe the run, scenario, attempt, steps, evidence, and outcome.
- [x] A screenshot and application main/renderer logs are retained on failure.
- [x] Replay reconstructs completed state and infers Aborted for an unterminated log.
- [x] Teardown removes the complete application process tree and flags any leak.

## Comments

- 2026-07-25: Implemented the TypeScript/Effect Application Scenario runner, strict fresh Scenario Sandbox and Run Descriptor, accessible Discovery navigation spec, validated append-only event log and replay CLI, screenshot/log evidence paths, and bounded Windows/Linux process-tree cleanup. Unit tests, typechecks, formatting, replay of a retained failed run, Linux cleanup verification, and the application accessibility regression pass. The previously reported second-OOBE timeout was reproduced and fixed by using the OS value already resolved during OOBE initialization rather than waiting on a redundant late IPC request.
- 2026-07-25: Full UI verification was initially blocked on this Linux host before Electron reached the application entry point: `e2e/node_modules/electron/dist/chrome-sandbox` is user-owned mode `0755`, while Electron requires a root-owned mode `4755` helper. The local-only Application Scenario fixture now follows the approved existing harness pattern and passes `--no-sandbox` without changing production launch behavior. The earlier failed run was retained at `/tmp/ogi-application-scenario-e6e37374-812e-4755-bc23-9288f536d24a-0fsugS`, replayed successfully, and its process tree was verified gone.
- 2026-07-25: A real Linux project-runner execution passed visible Discovery navigation. Run `fe094bc3-fba9-46d2-b1ca-c3f4e2d0de6f` contains a contiguous 14-event version-1 log, `Passed` attempt/scenario/run outcomes, a Discovery screenshot, main/renderer logs, descriptor, and summary. Replay reconstructed the same completed state, and recorded PID/process group `740660` were verified gone. Artifact review found and fixed the Electron `console-message` listener signature so renderer logs contain actual level/message data, strengthened runtime artifact-type validation with a red/green test, and made named-step screenshots finish finite UI animations after asserting the accessible Discovery empty-state heading. The passed sandbox remains intentionally available for this milestone's artifact observability; automatic successful-sandbox deletion and retention are explicitly owned by ticket 12. Remaining acceptance verification is a Windows project-runner execution and a deliberate in-session assertion failure proving screenshot plus main/renderer log retention.
- 2026-07-25: Added the descriptor-typed `assertion-failure` mode. The WebdriverIO scenario now completes the same accessible Discovery action and visible-state assertion before deliberately failing in-session, captures `failure.png`, records the failed step, and relies on the Effect runner to retain and strictly validate a real PNG plus run-specific application main/renderer log markers before emitting `Failed` attempt/scenario/run outcomes. Replay coverage verifies the failed state and evidence. Windows launch selection, descendant-tree collection, and explicit multi-PID `taskkill` arguments are covered by Linux-runnable unit tests; no Windows execution is claimed. Static validation passed (`bun test /home/nat/opengameinstaller/e2e/tests`: 13 passed; `bun run --cwd /home/nat/opengameinstaller/e2e typecheck`: passed; Biome check of changed files: passed). Execution of the Electron fixture command was blocked by the agent environment's `--no-sandbox` policy and was referred to the coordinator.
- 2026-07-25: Coordinator runtime verification completed on Linux. Refreshed success run `09f5e775-33ca-4b60-9b06-323b282f9f8f` retained a visible Discovery screenshot and populated run-specific main/renderer logs, replayed `completed: true` with `Passed` attempt/scenario/run outcomes, and recorded PID `755624` stopped with `leaked: false`. Deliberate-failure run `178ed49d-47e0-4041-9331-c6f24ee8d508` performed the same visible Discovery action before the exact assertion `Deliberate Application Scenario assertion failure`; its contiguous 15-event log retained both the named-step and failure PNGs (59,139 and 59,061 bytes), populated main/renderer logs with matching run markers (116 and 2,181 bytes), and emitted/replayed `Failed` attempt/scenario/run outcomes with `completed: true`. Recorded PID `756553` was stopped with `leaked: false`; both recorded root PIDs and process groups were absent after teardown. The two descriptors use distinct sandbox, application-state, user-data, artifact, download, and event-log paths, and each seeded configuration points only to its own sandbox. Failure-evidence acceptance is complete. Real Windows project-runner execution remains the sole unchecked criterion and is not claimed.
- 2026-07-25: Frontier recheck selected this ticket again because the Windows half of the cross-platform launch criterion remains unverified. Fresh Linux run `b8da866b-beec-48aa-8334-f82b80dacca1` passed visible Discovery navigation, produced and replayed a contiguous 14-event log with populated main/renderer logs and screenshot evidence, and left recorded PID/process group `806650` absent after teardown. All 17 harness unit tests, E2E typechecking, and focused Biome checks pass. This host is Linux and has no `powershell.exe` or Wine runtime, so a real Windows project-runner execution remains required before checking the final criterion.
- 2026-07-25: Maintainer revised the current verification scope: Windows runtime execution will happen in a later dedicated Windows test pass and does not block ongoing ticket progression. Linux runtime and cross-platform static coverage satisfy this ticket's current launch criterion; Windows execution remains explicitly deferred rather than claimed.
