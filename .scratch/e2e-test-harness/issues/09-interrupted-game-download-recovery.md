# 09 — Resume an interrupted game download

**What to build:** Let a user restart the application after deterministic termination during a fixture-game download, see the interrupted work, resume it through UI, and complete one correct installation without duplicated Library state.

**Blocked by:** 07 — Deliver the Golden Journey

**Status:** ready-for-review

- [x] The Fixture Service exposes a deterministic partial-download termination point.
- [x] The harness terminates the application process tree at that point without fabricating product state.
- [x] Relaunch presents the interrupted work through visible UI.
- [x] UI-driven resume produces the expected complete bytes.
- [x] Exactly one Library entry exists and stale partial state is removed.
- [x] Initial and resumed attempts appear in screenshots, logs, events, and reports.
- [x] Windows and Linux pass without public internet.

## Comments

- 2026-07-25: Implemented deterministic interrupted direct-download recovery in the packaged Product Journey. The Fixture Service holds the first transfer after exactly 65,536 of 262,144 bytes and supports a range response for the remainder. The harness verifies genuine persisted partial state, force-terminates the isolated application process group, relaunches the packaged application, presents the restored paused download in visible Downloads UI, resumes through the accessible Resume Download control, and uses the production downloader and addon setup path to finish installation.
- 2026-07-25: Linux run `58882d10-01a3-4dd1-862c-aabc812d9071` passed in 82 seconds with 55 contiguous events, eight passed named steps/screenshots, a `bytes=65536-` Fixture Service request returning 206, the exact 262,144-byte payload, exactly one `7001.json` Library record, no persisted in-progress or chunk files, and both the deliberately terminated application tree and final Product Journey tree recorded with `leaked: false`. Golden Journey regression `feaaf9a9-a7b4-460a-ad84-aea51ffa2acd` and the production-package boundary smoke also passed. Windows runtime remains deferred under the group policy; the Windows builder, descriptor, Job Object wrapper, and `taskkill /T /F` tree-termination path are statically covered and no public endpoint is used.
- 2026-07-25: Verification passed: 45 E2E unit tests, E2E TypeScript and Svelte checks with zero errors/warnings, application typechecking (existing stylesheet warnings only), updater build, CommonJS syntax checks, focused Biome checks, production package-boundary scan, and `git diff --check`.
- 2026-07-25: Review follow-up removed relaunch-time unhandled WebSocket rejections by matching production startup ordering: the Fixture Addon server starts first, restored addons start only after the relaunched renderer reports readiness, and runtime/connection events are sequenced after the client can reconnect. The Product Journey runner now scans collected product logs and fails on unexpected unhandled rejections, uncaught exceptions, unhandled-rejection codes, or fatal entries; focused coverage proves ordinary Chromium platform diagnostics remain non-fatal while an unhandled WebSocket rejection is caught.
- 2026-07-25: Clean Linux rerun `ac48d833-ae90-45fe-b2f0-eee7c86e4bc3` passed in 84 seconds with 55 contiguous events, all eight named steps/screenshots, the resumed 206 range request, exact final bytes, one Library entry, no stale partial state, no leaked process, and zero fatal/unhandled runtime-log matches. Golden Journey regression `517e1faa-9e48-46a2-afd1-ccd5a967d411` passed in 80 seconds under the new runtime-log gate. Final verification passes 46 E2E unit tests, E2E TypeScript/Svelte checks, focused Biome and CommonJS checks, production package-boundary smoke, and `git diff --check`. The earlier `58882d10-01a3-4dd1-862c-aabc812d9071` evidence is superseded because review found unhandled WebSocket rejections in its relaunch log.
- 2026-07-25: Final P1 process-safety follow-up removed every POSIX negative-PID signal. PID-tree termination now snapshots `pid`/`ppid`/`pgid`, refuses the harness or any harness ancestor, freezes only the selected root and descendants by positive PID until the tree is stable, and then force-terminates only those PIDs. This remains safe even when the product and harness share a process group. Focused tests cover shared-group identity, descendant-only planning, ancestor refusal, real nested-tree cleanup, harness survival, and Windows `taskkill /T /F` selection.
- 2026-07-25: Final scoped-termination run `4546f621-dba6-4db1-a048-cb5ea8e902de` passed in 89 seconds with 55 events, clean fatal/unhandled logs, exact resumed bytes, one Library entry, no stale state, all recorded product PIDs absent, and the invoking shell still healthy immediately afterward. Golden Journey `c8a72d17-0866-45ae-ab86-5b563070725f` passed in 82 seconds, and ticket 08 candidate-crash recovery `9f19141c-f6a2-4bc3-9bd4-1a3eb1715857` passed in 77 seconds under the same safe teardown. Final verification passes 48 E2E unit tests, E2E TypeScript/Svelte checks with zero errors/warnings, focused Biome and CommonJS checks, and `git diff --check`. This evidence supersedes the prior process-termination runs.
- 2026-07-25: Final orphan-process follow-up starts PID-tree tracking immediately after the WebdriverIO root launches, before waiting for its exit. The tracker retains Linux `/proc` start-time identities for PID-reuse safety, continuously snapshots descendants, retains the dedicated launch process-group identity, and discovers still-running group members after the root has exited and children have been reparented. Cleanup freezes, terminates, and verifies every identity that still matches, refuses harness ancestors, then scans stable logs only after tracked descendants are gone. A live regression now launches a root that creates a long-lived child and exits; tracked cleanup removes the orphan while the harness survives.
- 2026-07-25: Final tracked-descendant run `82b80baf-db78-4018-b414-a04dc5b8890a` passed in 89 seconds with 55 events, zero fatal/unhandled matches, no sandbox-associated process remaining after teardown, and a healthy invoking shell. Golden Journey `8f4c4d54-d4b9-4b52-a66f-ef090e931336` passed in 84 seconds and ticket 08 candidate-crash recovery `a5037932-9de2-4971-89de-7ea45c62b8ec` passed in 76 seconds with the same pre-exit tracking. Final verification passes 50 E2E unit tests, including the live root-exit/orphan case and PID-reuse identity parsing, plus E2E TypeScript/Svelte, focused Biome/CommonJS, and `git diff --check`. This evidence supersedes earlier teardown verification.
