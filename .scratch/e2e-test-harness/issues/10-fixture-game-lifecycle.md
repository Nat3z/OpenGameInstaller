# 10 — Complete the fixture-game lifecycle

**What to build:** Let a user discover and install a harmless fixture game, launch its visible platform window, close it, and uninstall it while proving unrelated sandbox data remains untouched.

**Blocked by:** 07 — Deliver the Golden Journey

**Status:** ready-for-review

- [x] The installed fixture game runs independently of host Bun or Node.
- [x] Launch occurs through application UI and produces a visible window plus sandbox marker.
- [x] Closing the fixture returns control cleanly to the application.
- [x] Uninstall occurs through visible UI.
- [x] Game files and its Library entry are removed.
- [x] Unrelated sentinel files remain unchanged.
- [x] Fixture-game descendants are contained and cleaned on Windows and Linux.

## Comments

- 2026-07-25: Implemented the complete packaged fixture-game lifecycle. The Fixture Addon now installs a platform launcher that invokes Electron directly rather than host Bun or Node, the application launches it from the visible Library Play control, and the fixture opens its own visible platform window, writes a sandbox marker, exposes a visible Close Game control, and returns the application Play control after clean exit. The lifecycle then uses visible Settings and Uninstall Game UI to remove the application-owned install directory and Library record while a sibling sentinel remains byte-for-byte unchanged. Production uninstall refuses paths outside the configured download root and symbolic-link install directories, and Linux game launch no longer routes executable paths with spaces through a shell.
- 2026-07-25: Linux Product Journey `7bfe64c6-c4f5-441c-ad73-f678f4683287` passed in 86 seconds with 60 contiguous events, ten passed named steps, eleven screenshots including the independently visible fixture window, exact fixture payload bytes, marker PID `1240967` with `visible: true`, clean return to Play, removed game directory and `7001.json`, unchanged sentinel content, and both recorded process roots absent after `leaked: false` teardown. Windows runtime remains deferred under the group policy; the Windows `.cmd` launcher, descriptor game-automation port, visible close path, and shared Windows descendant cleanup remain statically covered.
- 2026-07-25: Regression runs passed for the packaged Golden Journey (`e74b22de-16e5-4fb3-8051-134824da1d33`, 47 events) and interrupted game-download recovery (`8aa733a4-599d-45f9-ad4f-6c6b837bcdc5`, 62 events). Final verification passed all 50 E2E unit tests, E2E TypeScript/Svelte checks with zero errors or warnings, application TypeScript/Svelte checks and production build (existing stylesheet warnings only), updater build, focused Biome and CommonJS syntax checks, production-package boundary smoke with no active E2E hooks, and `git diff --check`.
- 2026-07-25: Review fixes replaced immediate destructive removal with a focused accessible confirmation dialog that names the target directory and offers distinct “Delete Files and Remove from Library” and “Remove from Library Only” actions. Deletion requires the explicit destructive action. IPC refusal is caught and retained in the dialog as a `role="alert"` instead of becoming an unhandled rejected promise. The Product Journey proves opening confirmation changes no state, an invalid ownership attempt is visibly refused without deleting files or Library state, and both the confirmation and refusal states pass targeted Axe scans.
- 2026-07-25: Install ownership now persists both the immutable install directory and its install-time download root. Deletion validates against that persisted root rather than current settings while preserving strict-descendant, realpath, and symbolic-link protections. Focused unit coverage changes the configured download root after installation and proves the owned install still uninstalls, while an unowned path is refused and retained.
- 2026-07-25: Superseding Linux lifecycle run `8a65fa40-2835-4037-89ac-d7f9a45fcb03` passed in 87 seconds with 66 contiguous events, twelve passed named steps, thirteen screenshots, visible confirmation and refusal evidence, exact payload and marker PID `1303093`, unchanged sentinel data, removed game and Library state, and both recorded process roots absent after `leaked: false` teardown. Review regressions passed for Golden Journey `5218c2d0-97af-4f9d-afd3-8466f925b3cc` (47 events) and interrupted recovery `115d9eb4-f259-4d56-a61d-1cb62514d0e8` (62 events). Final checks pass 51 E2E unit tests, E2E and application typechecks, application/updater/Fixture Addon builds, focused Biome and CommonJS checks, the production-package boundary, and `git diff --check`. The broader application accessibility command was also attempted; its Welcome state passed, while its pre-existing Community Addons state remains red on unrelated contrast and heading-order violations.
- 2026-07-25: Final security re-review consolidated deletion into one production helper used by both `registerLibraryHandlers` and the packaged Fixture Service runtime. The helper rejects symlink/junction/reparse substitution in the persisted ownership root and every existing component through the install directory before realpath containment and recursive deletion. Direct regression coverage replaces the ownership root with a symlink/junction to an unrelated directory and separately substitutes an intermediate component; both attempts are refused and the unrelated sentinel remains unchanged. The fixture fallback no longer contains a duplicate deletion authorization path.
- 2026-07-25: Final shared-helper lifecycle run `1a06ef98-a28e-4441-8809-f806ff493520` passed with 66 events, all targeted confirmation/refusal Axe checks, both recorded roots absent, and no leaked process. Golden Journey `592775b4-e927-4f6f-aa70-af39df6705f4` passed with 47 events, and ticket 09 interrupted recovery `572bfd32-ed5c-4040-8a9a-38a4db310793` passed with 62 events and all three recorded process roots absent. Final verification passes 51 E2E unit tests, focused root/component substitution coverage, E2E and application typechecks, application/updater/Fixture Addon builds, focused Biome/CommonJS checks, the production-package boundary, and `git diff --check`.
