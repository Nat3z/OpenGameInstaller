# 11 — Prove offline updater and application behavior

**What to build:** Show that an offline user can launch the Last Known-Good Installation through the updater, browse Library, and launch an installed fixture game without either product making unexpected remote requests.

**Blocked by:** 08 — Recover the Last Known-Good Installation; 10 — Complete the fixture-game lifecycle

**Status:** ready-for-review

- [x] Offline updater skips update work and launches the Last Known-Good Installation.
- [x] Offline application presents its offline state without disabling Library.
- [x] The installed fixture game launches successfully while offline.
- [x] The Fixture Service rejects and records any unexpected request.
- [x] Zero unexpected traffic is asserted rather than inferred.
- [x] Windows and Linux expose the complete flow in Observer and reports.

## Comments

- 2026-07-25: Implemented the offline Product Journey with a seeded current Last Known-Good Installation and installed fixture game. The updater visibly enters Offline Mode, skips release and artifact work, and launches the working application with `--online=false`. The application now resolves and presents offline state before addon queries, keeps Library enabled, and drives the installed fixture game from visible Library UI through its independently visible window and Close Game control.
- 2026-07-25: Added an E2E-only updater/application traffic guard that records Node fetch and Electron session traffic, deterministically denies non-loopback HTTP(S)/WebSocket requests, and retains per-product JSONL logs. The Fixture Service marks every unsupported route as unexpected and returns 404. The runner performs a final explicit zero-traffic assertion over both guard logs and Fixture Service history rather than inferring silence.
- 2026-07-25: Linux run `3fe2db30-184a-425d-974a-efe173826c88` passed with 37 contiguous events, four passed named steps, five screenshots including the visible fixture-game window, zero Fixture Service requests, zero denied or unexpected traffic, two explicitly allowed loopback SDK requests, a populated HTML report, and `leaked: false` cleanup with both recorded process roots absent. Packaged Golden Journey regression `4350235c-fe60-489a-bfdf-a6a18518cb31` passed with 47 events and all seven steps. Windows uses the same packaged descriptor, offline guard, Last Known-Good launch, fixture launcher, UI steps, event/report path, and Windows process-tree cleanup; runtime remains deferred under the ticket-group policy and is not claimed.
- 2026-07-25: Final verification passed all 53 E2E unit tests, E2E TypeScript/Svelte checks with zero errors or warnings, application TypeScript/Svelte checks (existing stylesheet warnings only), application and updater builds, focused Biome and CommonJS syntax checks, the production-package boundary with no active E2E hooks, and `git diff --check`.
- 2026-07-25: Review fixes replaced descriptor-owned offline decisions with shared production helpers. The updater production entry and packaged Product Journey both call `decideUpdaterStartup`, and the application production handler and packaged Product Journey both call the same requested/effective online-state implementation. Direct regressions prove CLI-offline, network-offline, and online decisions. Runtime handoff evidence records the production updater action `skip-update-and-launch-offline` and application reason `cli-offline`.
- 2026-07-25: The traffic guard now uses an explicit host/port allowlist and fail-closed observation across `fetch`, Node HTTP/HTTPS, raw TCP, TLS, DNS, Electron session HTTP/WebSocket traffic, and inherited Node transports in application, addon, and fixture-game descendants. Every attempt is logged as expected or unexpected; unsupported loopback ports are denied. A child-process probe proves unexpected Node HTTP, HTTPS, and loopback traffic is rejected. Guard-install evidence is required for the updater, launched application, and fixture-game PIDs before the zero-unexpected-traffic assertion can pass.
- 2026-07-25: Production-boundary evidence is now always retained and linked in the Run Event Log/report, and any active hook fails the journey. A temporary packaged-input regression proves `OGI_RUN_DESCRIPTOR` is detected and rejected. Superseding Linux offline run `4216173e-a55e-4253-9998-f0ed2193e284` passed with 38 contiguous events, four passed steps, five screenshots, zero Fixture Service requests, zero unexpected traffic, explicit guard installation in five product/descendant PIDs including application PID `1440477` and fixture-game PID `1440612`, and no leaked recorded roots. Golden Journey regression `57a763fc-6f5f-4fb8-b356-c7bbe961315a` passed with 48 events and retained production-boundary evidence.
- 2026-07-25: Superseding verification passes all 55 E2E unit tests (227 assertions), E2E TypeScript/Svelte checks with zero errors or warnings, application TypeScript/Svelte checks (existing stylesheet warnings only), application and updater builds, focused Biome and CommonJS checks, production-package boundary with no active E2E hooks, and `git diff --check`. Windows runtime remains deferred under policy and is not claimed.
- 2026-07-25: Final review-close run `4f790c99-6a1a-4ff6-9c51-e8149d993002` supersedes the prior offline runtime after moving the production updater's SDK-port probe behind its shared online startup decision and making descendant `NODE_OPTIONS` propagation safe for paths containing spaces. It passed with 38 events, zero Fixture Service or unexpected traffic, five guarded product/descendant PIDs, complete application and fixture-game guard coverage, retained production-boundary evidence, and no leaked process.
- 2026-07-25: The final High review fix moved TCP enforcement to `net.Socket.prototype.connect`, including Node's normalized-argument array and public options/port/host/path overloads, so both `net.connect`/`createConnection` and direct `new net.Socket().connect()` calls pass through one guard without duplicate allowed records. Direct `TLSSocket` prototype connects are classified separately when applicable, while `tls.connect` suppresses nested prototype double-counting. Child-process regressions prove disallowed direct TCP/TLS prototype calls and unexpected loopback are denied/logged, while one allowlisted module-level TCP call produces exactly one CONNECT record.
- 2026-07-25: Superseding offline run `5cb31cd4-f312-4c5d-aa8a-619d0a89621d` passed with 38 events, zero Fixture Service requests, zero unexpected traffic, five guarded PIDs including application PID `1475957` and fixture-game PID `1476123`, no missing guard coverage, retained production-boundary evidence, and no leaked roots. Offline startup no longer starts installed addons, preventing unnecessary offline runtime connections. Golden Journey `66ef0be8-c51c-475c-94d5-a0f45c467350` passed with 48 events. Final verification passes 55 E2E tests with 232 assertions, E2E typechecking, focused Biome/CommonJS checks, production-boundary smoke with no active hooks, and `git diff --check`.
- 2026-07-25: The final Medium review fix recognizes `http.request`/`get` and HTTPS options carrying `socketPath` as Unix-domain or Windows named-pipe IPC before URL classification. These calls bypass network logging and denial while ordinary `path` remains an HTTP request path and all host/port network behavior remains guarded. Static coverage includes Unix and `\\.\pipe\...` semantics; a child process runs a real local Unix-domain HTTP server and completes both `get(options, callback)` and `request(options, callback)` without producing any network record beyond guard installation.
- 2026-07-25: Final superseding offline run `16574028-09db-4470-b204-2ec33a40c142` passed with 38 events, zero fixture or unexpected traffic, five guarded PIDs including application `1501462` and fixture game `1501654`, complete guard coverage, retained boundary/report evidence, and no leaked process. Golden Journey `2c3181ab-9e41-43b4-a46a-8406230eea9d` passed with 48 events. All 55 E2E tests pass with 238 assertions; E2E typechecking, focused Biome/CommonJS, production-boundary smoke, builds, and `git diff --check` pass.
- 2026-07-25: Closing-review follow-up now gives a second `options.socketPath` precedence over URL/string input for every HTTP/HTTPS request/get overload, matching Node's URL-first IPC behavior. The real Unix-domain child probe exercises options-first and URL-first `get` and `request` callbacks; static coverage verifies Windows named-pipe URL-first behavior. All IPC calls remain unlogged as network traffic.
- 2026-07-25: UDP is now fail-closed through `dgram.Socket.prototype.connect` and `send`, including options, port/address, offset/length, callback, connected-socket, IPv4, and IPv6 forms. Connected destinations are tracked per socket. Non-literal hostnames are rejected before custom lookup can redirect an allowlisted name; internal `0.0.0.0`/`::` bind lookups remain local and unlogged. Child probes prove unexpected send/connect and custom-lookup attempts are denied, while exact IPv4/IPv6 allowlisted sends and connected sends succeed and are recorded.
- 2026-07-25: Final superseding offline run `c0babbca-b5ca-4fa3-a085-382ec56763b6` passed with 38 events, zero Fixture Service or unexpected traffic, five guarded PIDs including application `1530923` and fixture game `1531125`, complete guard coverage, retained boundary/report evidence, and no leaks. Golden Journey `94efa731-9429-4061-bfa0-4b67344fbee0` passed with 48 events. All 55 E2E tests pass with 243 assertions; typecheck, focused lint/syntax, production-boundary smoke, builds, and `git diff --check` pass.
- 2026-07-25: DNS closure now dynamically intercepts every exported callback and promise resolution API plus callback/promise `Resolver` methods across their prototype chains, including future `resolve*` exports, `reverse`, `lookupService`, and `resolveTlsa`. Literal-IP `lookup` and internal wildcard bind lookup remain local-only. `setServers` accepts only literal DNS server host/port pairs present in the exact allowlist; unallowlisted/default/hostname servers are denied and logged before native resolver traffic.
- 2026-07-25: An adversarial child probe reproduces the review case with a UDP DNS listener: callback and promise Resolver `setServers`/`resolve4`, module `setServers`, `reverse`, `lookupService`, and `resolveTlsa` are denied with zero packets reaching the listener. An exact allowlisted `127.0.0.1:5353` server configuration succeeds, while all subsequent resolution remains fail-closed. Final offline run `ebbb8a7c-11f3-4085-9a68-de0b3e00fa3f` passed with 38 events, zero unexpected traffic, five guarded PIDs including application `1550002` and fixture game `1550188`, complete coverage, and no leaks. Golden Journey `ba911458-e36d-4768-a598-fd90a5546f06` passed with 48 events. All 55 E2E tests pass with 246 assertions; typecheck, focused lint/syntax, boundary smoke/builds, and `git diff --check` pass.
- 2026-07-25: Final offline addon-runtime review fix prevents the Fixture Service from loading or starting addon/download runtimes when the production effective online state is false and returns an immediately-ready, idempotent no-op close without emitting false addon-runtime readiness. Offline PlayPage launch and task discovery plus GameManager post-launch now skip addon hooks/queries. Two clean ticket runs `7e430d18-a1b4-4105-815b-e51900475381` and `ed9114a9-92b5-46fc-958c-d1f067b4b65d` each passed with 38 events, four steps, zero Fixture Service/unexpected traffic, five guarded PIDs, explicit pre-launch and task-query skip evidence, zero WebSocket unhandled errors, and no leaks. Golden Journey `9eb1aff1-7474-4393-af0d-4f675fc3725c` passed with 48 events and seven steps. Final verification passes all 56 E2E tests with 247 assertions, E2E typecheck with zero errors/warnings, application typecheck with zero errors and the existing 435 stylesheet/Svelte warnings, application/updater builds, focused Biome/CommonJS checks, production-boundary smoke with no active hooks, and `git diff --check`.
