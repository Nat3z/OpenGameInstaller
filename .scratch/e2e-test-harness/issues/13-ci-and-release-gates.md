# 13 — Gate pull requests and releases

**What to build:** Give contributors required Windows and Linux checks that run accessibility, fast product smoke, and the packaged Golden Journey, while nightly and release workflows publish useful reports and enforce reliability budgets.

**Blocked by:** 07 — Deliver the Golden Journey; 08 — Recover the Last Known-Good Installation; 09 — Resume an interrupted game download; 10 — Complete the fixture-game lifecycle; 11 — Prove offline updater and application behavior; 12 — Enforce retries, outcomes, retention, and process hygiene

**Status:** ready-for-review

- [x] Pull requests run both accessibility gates, unpackaged Application/Updater smoke, and packaged Golden Journey on Windows and Linux.
- [x] Failed, Flaky, accessibility, budget, leaked-process, and infrastructure outcomes fail required checks.
- [x] Nightly runs the broader deterministic failure and quarantined scenario matrix.
- [x] Release workflow runs production packaging smoke and the full deterministic suite.
- [x] HTML and machine-readable reports link retained CI artifacts.
- [x] Ordinary UI, transfer, Golden Journey, PR-job, and full-job budgets are enforced and reported.
- [x] CI never reads real user state or requires public internet for deterministic scenarios.

## Comments

- 2026-07-25: Added required pull-request/main Windows and Linux jobs for both product/Observer accessibility gates, unpackaged Application and Updater Scenarios, and the packaged Golden Journey. Added scheduled Windows/Linux nightly coverage for the deterministic recovery, lifecycle, offline, and quarantined-scenario matrix. Tagged releases now wait for a Windows/Linux release E2E job that runs production-package boundary smoke plus the full current deterministic suite before GitHub release creation.
- 2026-07-25: Added a deterministic-only CI suite runner with pinned per-run evidence, machine-readable `ci-summary.json`, HTML/Markdown reports, GitHub artifact links, and failing propagation for every non-passing required-check result. It enforces and reports 30-second ordinary UI steps, two-minute fixture transfers, five-minute Golden Journeys, ten-minute PR jobs, and 25-minute nightly/release jobs. Every workflow uses a runner-owned Scenario Sandbox root, read-only repository permissions, no Live Service Scenario or credential path, and the existing Fixture Service-backed deterministic commands.
- 2026-07-25: Linux runtime verification passed the Application Scenario, Updater Scenario, and packaged Golden Journey through the new required-suite runner with no budget violations. The initial combined run correctly failed on a pre-existing Community Addons accessibility regression; fixing the heading order and selected-card source contrast made the complete application/updater/Observer accessibility command pass. Static verification passed all 211 E2E tests (887 assertions), E2E TypeScript/Svelte checks with zero errors or warnings, application typechecking with existing stylesheet warnings only, application/updater builds, production-package boundary smoke with no active E2E hooks, focused Biome, YAML parsing for all affected workflows, quarantine discovery, and `git diff --check`. Windows runtime remains deferred under project policy; both Windows matrices and static cross-platform paths are configured and covered without claiming execution.
- 2026-07-25: Independent-review fixes moved concurrency to workflow-valid contexts and were verified with actionlint 1.7.7, increased outer workflow limits to 15 minutes for the internally bounded 10-minute PR suite and 35 minutes for the internally bounded 25-minute full suite, and persist deterministic artifact names plus run-artifacts URLs in JSON, Markdown, and visible HTML before upload. Tagged release verification now depends on the build matrix, downloads the exact `windows-assets` or `linux-assets` publication artifact, and gates publication on bounded extraction/installation, packaged-resource/ASAR scanning, Chromium readiness, and process-tree cleanup. Manual release runs build and consume the same named publication outputs.
- 2026-07-25: Nightly quarantine handling now loads a validated registry, checks matching source annotations and future expiry, executes every registered command with forced-quarantine context, retains stdout/stderr/typed result evidence, accepts only the expected failure, fails unexpected behavior, and flags unexpected passes for dequarantine. An empty registry reports `No Quarantines` rather than claiming scenario execution; focused fixtures cover expected failure, unexpected pass, unexpected status, expired/mismatched/duplicate/missing registrations.
- 2026-07-25: Review-fix verification passed 223 E2E tests (918 assertions), focused CI/quarantine/package regressions, E2E TypeScript and Observer checks with zero errors or warnings, application typechecking with existing stylesheet warnings only, production source-boundary verification, application and updater publication builds, Biome, actionlint, and `git diff --check`. Actual Linux publication smoke passed against `OpenGameInstaller-linux-pt.AppImage` and `OpenGameInstaller-Setup.AppImage`: both artifacts extracted, their ASAR/resources contained no active E2E hooks, both reached Chromium readiness, shut down without leaked product processes, and wrote only isolated state. Windows runtime remains deferred; Windows portable ZIP/NSIS artifact selection, isolated installation, extraction, readiness, cleanup, and workflow consumption paths have static tests/typechecking and actionlint coverage without claiming execution.
- 2026-07-25: A prior aggregate verification attempt hit `EDQUOT` while generated AppImage extractions and E2E evidence exhausted the environment quota. Obsolete generated `/tmp/ogi-*` data was pruned and verification was rerun successfully; the quota event is recorded as environment evidence, not a product test result.
- 2026-07-26: Second-review upload fixes set `include-hidden-files: true` on every required, nightly, manual-release, and tagged-release retained-report upload, with workflow policy coverage. Both release workflows now collect the complete four-file platform publication manifest, generate manual-release blockmaps, and install the Linux isolation/audit tools. Package smoke requires both adjacent gzip blockmaps, validates their structure and described sizes, regenerates each blockmap with `app-builder` to prove checksum association with the exact executable, and reports both blockmap identities.
- 2026-07-26: Production package runtime smoke now launches the application with the supported `--online=false` path and updater with the supported `--gui` channel-picker path. Linux runs each unchanged extracted package inside a bubblewrap network namespace with DNS attempts disabled, traces all network syscalls with `strace`, fails unexpected public connections, and cleans the isolated process group. Windows runtime remains deferred; the static Windows path installs an executable-scoped outbound firewall rule around each portable/installed package launch and removes it during cleanup. Actual Linux publication smoke passed for both AppImages with Chromium readiness, no active package hooks, valid associated blockmaps, empty public-traffic audits, isolated state, and no leaked processes; compact evidence is retained under `.scratch/e2e-test-harness/evidence/ticket-13-review2`.
- 2026-07-26: Quarantine execution now requires versioned typed outcome evidence and the registry's exact assertion ID/signature. Only that clean status-1 assertion is accepted; missing/malformed evidence, unrelated assertions, Flaky, Infrastructure Failed, leaked processes, timeout, signal, other statuses, and unexpected passes are separately retained and fail the nightly matrix, with unexpected passes requesting dequarantine. Verification passed all 226 E2E tests (954 assertions), E2E TypeScript/Svelte checks with zero errors or warnings, application typechecking with existing stylesheet warnings only, application/updater builds, production boundary scanning, actionlint, focused Biome for all ticket-13 code, and `git diff --check`. Repository-wide Biome still reports pre-existing formatting/import-order drift in unrelated application and generated package files; those files were not changed for this review fix.
- 2026-07-26: Third-review Windows hardening places the NSIS installer itself behind a verified executable-scoped outbound firewall rule before silent local installation, records typed installation/isolation lifecycle evidence, removes the rule in `finally`, and fails when installation, removal, or either proof step fails. Packaged application/updater launch now keeps rule creation, spawn and asynchronous spawn-error observation, readiness, process-tree termination, rule deletion, and deletion proof inside one lifecycle. Static unit coverage verifies installer and asynchronous spawn-error ordering plus typed cleanup-failure evidence. Windows runtime remains deferred and is not claimed as executed.
- 2026-07-26: Quarantined commands now launch through the existing immediate process-tree tracker/subreaper containment, retain linked `process-evidence.json`, perform bounded emergency tree cleanup on every outcome, and classify timeout, tracked descendant leaks, cleanup infrastructure failure, signal, and assertion evidence independently. A live Linux regression starts a root plus detached descendant, times out the root, proves the detached PID is tracked and removed, and records zero post-cleanup survivors. A process-root reaping wait also removes a full-suite timing race. Final verification passed 230 E2E tests (966 assertions), the focused 18-test third-review suite (59 assertions), E2E typechecking with zero errors or warnings, application typechecking with existing warnings only, application/updater builds, the production package boundary, actionlint 1.7.7, focused Biome, the empty real quarantine matrix (`No Quarantines`), `git diff --check`, and a leaked-process scan. Actual Linux publication smoke reran against both AppImages and associated blockmaps: both extracted unchanged artifacts reached Chromium readiness under isolated audited networking, exposed no active E2E hooks or public traffic, and left no product processes. Compact evidence is retained under `.scratch/e2e-test-harness/evidence/ticket-13-review3`.
- 2026-07-26: Final-review Windows quarantine containment now constructs every registered command through the existing suspended-launch Job Object wrapper instead of generic root-PID tracking. The wrapper applies `KILL_ON_JOB_CLOSE` before resuming the command, owns the scenario timeout, queries all active Job Object PIDs after the root exits or times out, writes version-2 root/active/survivor/timeout/kill-on-close evidence before closing the Job Object, and then closes the job to terminate every remaining member even if the command root already exited or reparented descendants. Missing, malformed, or unproven Job Object evidence is Infrastructure Failed; any unexpected descendant observed before close is Leaked Process; clean Job Object timeout remains Timed Out. Linked process/result evidence retains the exact observed PIDs and Windows result path without depending on root existence. Static parser, command-construction, classification, and cleanup-ordering tests cover the deferred Windows path while the live Linux detached-child regression remains unchanged. Final verification passed 234 E2E tests (981 assertions), 23 focused containment tests (162 assertions), E2E typechecking with zero errors or warnings, application typechecking/build and updater build, production package boundary scanning, actionlint 1.7.7, focused Biome, `git diff --check`, and the real empty quarantine matrix. Actual Linux publication smoke again passed for both AppImages and associated blockmaps with readiness, isolated audited networking, no active hooks/public traffic, and no leaked processes; compact evidence is retained under `.scratch/e2e-test-harness/evidence/ticket-13-review4`. Windows runtime remains deferred and is not claimed as executed.
