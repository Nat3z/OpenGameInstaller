# 15 — Exercise deterministic torrent installation

**What to build:** Let a user discover and install a fixture game through a local torrent or magnet source, using the same visible workflow and evidence as direct download without depending on a public swarm.

**Blocked by:** 07 — Deliver the Golden Journey

**Status:** ready-for-review

- [x] A loopback-only deterministic torrent fixture is available on Windows and Linux.
- [x] Discovery, source selection, progress, setup, and Library verification occur through visible UI.
- [x] No public tracker, peer, or credentialed provider is contacted.
- [x] Installed bytes match the fixture payload and exactly one Library entry exists.
- [x] Timeout, process cleanup, screenshots, logs, and event evidence follow the shared harness rules.

## Comments

- 2026-07-26: Added a private loopback HTTP tracker, TCP seeder, deterministic torrent metadata, and exact fixture payload transfer through the production WebTorrent handler and manager. DHT, LSD, uTP, WebRTC, UDP/WebSocket trackers, UPnP, and PMP are disabled; strict Run Descriptor validation rejects non-loopback fixture, tracker, peer, and sandbox values. Linux uses the real packaged Product Journey, while deferred Windows runtime is covered by static/unit assertions for the materialized `OpenGameInstaller.exe` path and bundled torrent runtime.
- 2026-07-26: The retained Linux run `0c053d71-1f29-4c5b-894d-c8b24c5ea48e` passed first-run WebTorrent selection, visible discovery and source selection, nonzero progress, setup, seeding, and exactly one Library entry. Its containment artifact reports zero unexpected traffic with the guard, local tracker, local peer, and torrent metadata request all observed. Named screenshots, application/updater logs, traffic logs, Run Events, HTML report, retention manifests, and process/fixture stop evidence are retained under `/tmp/ogi-e2e-runs/product-journey-reliable-0c053d71-1f29-4c5b-894d-c8b24c5ea48e-zT4Ted`.
- 2026-07-26: Final verification passed the deterministic torrent Product Journey, packaged Golden Journey, fixture lifecycle regression, 240 E2E tests (1,063 assertions), 46 updater tests (308 assertions), application and E2E typechecks, application/updater builds, production-package boundary scanning, scoped Biome, JavaScript syntax checks, `git diff --check`, closed tracker/peer ports, and a zero-leak product/Electron/WebTorrent/tracker/addon process scan. Existing application Svelte/style warnings remain unchanged.
- 2026-07-26: Independent-review remediation now contains every torrent network surface. Linux launches the complete attempt tree under Bubblewrap `--unshare-net`; retained runtime evidence proves a distinct network namespace with only `127.0.0.1` and `::1`, a tracker bound to `127.0.0.1`, wildcard WebTorrent seeder/application listeners confined inside that namespace, and no externally reachable wildcard listener. Windows runtime remains deferred, with static/unit coverage for temporary Bun/Electron inbound and outbound Windows Firewall block rules, program scope, all-profile/remote-address scope, and unconditional `finally` cleanup.
- 2026-07-26: Nightly and release suites now include the pinned `ci:deterministic-torrent-installation` command with a 300-second internal budget, child failure and timeout propagation, and required Run Event artifacts for containment, listener isolation, and payload-manifest verification. The Linux command refuses network driver discovery and resolves only an explicit or version-compatible pre-cached Chromedriver before entering the isolated namespace.
- 2026-07-26: Payload verification now defines the complete two-file torrent manifest with literal paths, sizes, and SHA-256 digests; validates torrent metadata and the downloaded source tree; and validates the exact installed three-file tree, generated launcher bytes, stable Library install directory/launch path, and exactly one Library entry. Regressions reject missing, extra, truncated, and substituted files, including corruption of the second payload file.
- 2026-07-26: Final post-review retained Linux torrent CI run `0b32c2a5-e547-43f1-b7eb-8d57639ce520` passed under `/tmp/ogi-e2e-runs/product-journey-reliable-0b32c2a5-e547-43f1-b7eb-8d57639ce520-7fkzCD`. Its isolation artifact reports distinct namespace IDs, no non-loopback address, tracker `127.0.0.1`, wildcard peers confined to the namespace, `externallyReachableWildcardListener: false`, and `passed: true`; containment reports no unexpected traffic; payload evidence records exactly `fixture-game.cjs`, `golden-journey.txt`, and `fixture-game.sh` with exact sizes and SHA-256 digests.
- 2026-07-26: Post-review verification passed the pinned torrent CI command, packaged Golden Journey (`7ee8f7eb-22d1-4fda-acf1-002f2d20ea7f`), fixture lifecycle (`a14ac5be-9493-42dc-91e1-7ecb142b58ee`), 244 E2E tests (1,087 assertions), 46 updater tests (308 assertions), 179 focused ticket/reliability tests (753 assertions), application and E2E typechecks, application/updater builds, production-package boundary scanning, scoped Biome, JavaScript and PowerShell syntax checks, `git diff --check`, embedded network/listener evidence, and a zero-leak product/Electron/WebTorrent/tracker/addon process scan. No ticket 15 remediation changed workflow YAML, so actionlint was not applicable to this review pass; existing application Svelte/style warnings remain unchanged.
- 2026-07-26: Second-review remediation explicitly adopts the containment, isolation, and payload-manifest assertion files into the reliable aggregate Run Event Log. Each top-level artifact event has a contained `attempt-N/...` relative link and numeric attempt association; the aggregate HTML renders those links with `Attempt N`, and `reliability.json` includes the complete `attemptArtifacts` summary. Nightly/release required-artifact classification now reads only newly created top-level aggregate logs rather than relying on recursive attempt-log discovery. Unit coverage proves all three required assertions are discoverable from that public aggregate surface.
- 2026-07-26: Torrent Library verification now counts the complete visible `[data-library-item]` collection and requires exactly one item representing Golden Journey Fixture. It independently enumerates the complete `application-state/library` directory, requires exactly one real `7001.json` file, rejects extra and hidden files or unsupported entries, rejects malformed JSON, and validates the only record's source, stable install directory, and exact launcher path. Extra-card, extra-file, hidden-file, and malformed-record regressions pass.
- 2026-07-26: Final second-review Linux torrent CI run `bf600e9a-c855-4039-b550-c57efbaef57f` passed under `/tmp/ogi-e2e-runs/product-journey-reliable-bf600e9a-c855-4039-b550-c57efbaef57f-kZoPB0`. Its top-level `events.jsonl`, `report.html`, and `reliability.json` all expose the three required assertion artifacts with `attempt: 1`; payload evidence reports `visibleLibraryItems: 1`, `libraryRecords: 1`, and only `library/7001.json`. Golden Journey `e0435bb5-7089-4b4f-8d2d-ff6d2fb49d28` and fixture lifecycle `72196717-b82c-4cf8-9040-bd67fbb9053c` also passed.
- 2026-07-26: Second-review verification passed 247 E2E tests (1,101 assertions), 46 updater tests (308 assertions), 182 focused ticket/reliability tests (765 assertions), application and E2E typechecks, application/updater builds, production-package boundary scanning, scoped Biome, JavaScript syntax checks, `git diff --check`, top-level CI evidence discovery against the retained aggregate log, closed torrent/product listeners, and a zero-leak product-process scan. Existing application Svelte/style warnings remain unchanged.
- 2026-07-26: Final-review remediation validates the complete persisted `7001.json` schema with no unknown or missing fields. It requires exact fixture `appID`, storefront, version, addon source, and name; binds `installRoot` to the Scenario Sandbox `downloads` directory; requires exact install directory, working directory, and launcher paths; and accepts capsule/cover images only at the exact loopback Fixture Service HTTP origin, allocated port, and `/images/golden-journey.svg` path. Field-by-field regressions reject every missing field, every semantic substitution, path escapes, public/wrong image URLs, wrong ID/version/storefront/source/name, and unknown fields.
- 2026-07-26: Final-review Linux torrent CI run `857fcedb-cefe-4028-b9bf-0ddd13f35a64` passed under `/tmp/ogi-e2e-runs/product-journey-reliable-857fcedb-cefe-4028-b9bf-0ddd13f35a64-ceCdg9`. Its retained Library record contains exactly the expected 11 fields and sandbox paths, with both images bound to `http://127.0.0.1:38395/images/golden-journey.svg`; containment and namespace-isolation assertions passed, and exact payload/launcher bytes remain verified. Packaged Golden Journey `820b19ce-6b45-41b7-98e8-840cb72eba4e` and fixture lifecycle `e0967333-5b9d-496d-8aed-2e767ecc13fd` also passed.
- 2026-07-26: Final-review verification passed 247 E2E tests (1,122 assertions), 46 updater tests (308 assertions), 182 focused ticket/reliability tests (788 assertions), application and E2E typechecks, application/updater builds, production-package boundary scanning, changed-file Biome, JavaScript syntax checks, `git diff --check`, closed tracker/peer/Fixture Service/application listener ports, and a zero-leak product/WebTorrent/tracker/addon process scan. The first Golden Journey retry encountered only the known generated-run disk quota condition; superseded retained runs were removed and the clean retry passed. Existing application Svelte/style warnings remain unchanged.
